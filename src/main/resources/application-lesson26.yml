spring:
  sql:
    init:
      schema-locations:
        - classpath:schema.sql
        - classpath:schema-lesson26.sql
      data-locations:
        - classpath:data.sql
        - classpath:data-lesson26.sql
